This blog is is build with the Leptos Client Side Rendering framework in the language Rust.

To build this application, clone or fork the repo, install the trunk wasm service cli. 
rustup install nightly
rustup target add wasm32-unknown-unknown
trunk serve

In your web browser, navigate to localhost:3000/blog
And you should see the webpage displayed
